[defaults]
inventory = inventory/hosts.ini
remote_user = ubuntu
private_key_file = ~/.ssh/wordpress-aws
host_key_checking = False
vault_password_file = ~/.ansible_vault_pass
interpreter_python = auto_silent
retry_files_enabled = False

[ssh_connection]
pipelining = True
